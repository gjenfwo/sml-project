{
 "iio": [
  {
   "aliases": [
    "gyroscope/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based gyroscope input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a gyroscope reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a gyroscope configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/gyroscope",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "direction-vector",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "x": 0,
       "y": 0,
       "z": 0
      },
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "direction-vector",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "x": 0,
       "y": 0,
       "z": 0
      },
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of direction-vector packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_gyroscope_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "direction-vector",
     "description": "Angular speed in all X/Y/Z axes, in radians per second.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "direction-vector",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "direction-vector",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_GYROSCOPE",
   "version": "1"
  },
  {
   "aliases": [
    "magnetometer/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based magnet input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a magnet reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a magnet configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/magnetometer",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "direction-vector",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "x": 0,
       "y": 0,
       "z": 0
      },
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "direction-vector",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "x": 0,
       "y": 0,
       "z": 0
      },
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "direction-vector",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "x": 0,
       "y": 0,
       "z": 0
      },
      "description": "Oversampling ratio measurement of the sensor",
      "name": "oversampling_ratio",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_magnetometer_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "direction-vector",
     "description": "Magnet location in all X/Y/Z axes, in Gauss.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "direction-vector",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "direction-vector",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_MAGNETOMETER",
   "version": "1"
  },
  {
   "aliases": [
    "thermometer/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based temperature input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a temperature reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a temperature configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/thermometer",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset option.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "Oversampling ratio measurement of the sensor",
      "name": "oversampling_ratio",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_thermometer_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "float",
     "description": "Temperature read, in milli degrees Celsius",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_THERMOMETER",
   "version": "1"
  },
  {
   "aliases": [
    "pressure-sensor/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based pressure input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a pressure reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a pressure configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/pressure-sensor",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "Oversampling ratio measurement of the sensor",
      "name": "oversampling_ratio",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_pressure_sensor_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "float",
     "description": "Pressure data read in Kpa.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_PRESSURE_SENSOR",
   "version": "1"
  },
  {
   "aliases": [
    "color-sensor/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based color input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a color reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a color configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/color-sensor",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device Red raw readings",
      "name": "scale_red",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device Green raw readings",
      "name": "scale_green",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device Blue raw readings",
      "name": "scale_blue",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device Red raw readings",
      "name": "offset_red",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device Green raw readings",
      "name": "offset_green",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device Blue raw readings",
      "name": "offset_blue",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of direction-vector packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_color_sensor_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "rgb",
     "description": "Color Sensor data read.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE_RED",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE_GREEN",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE_BLUE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 4,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET_RED",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 5,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET_GREEN",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 6,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET_BLUE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 7,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_COLOR_SENSOR",
   "version": "1"
  },
  {
   "aliases": [
    "accelerometer/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based accelerate input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a accelerate reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a accelerate configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/accelerometer",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "direction-vector",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "x": 0,
       "y": 0,
       "z": 0
      },
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "direction-vector",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "x": 0,
       "y": 0,
       "z": 0
      },
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of direction-vector packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_accelerometer_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "direction-vector",
     "description": "Accelerate data read, in m/s^2.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "direction-vector",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "direction-vector",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_ACCELEROMETER",
   "version": "1"
  },
  {
   "aliases": [
    "humidity-sensor/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based humidity input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a humidity reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a humidity configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/humidity-sensor",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_humidity_sensor_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "float",
     "description": "Relative humidity data read, in percentage(%rh).",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_HUMIDITY_SENSOR",
   "version": "1"
  },
  {
   "aliases": [
    "adc/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based adc input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a adc reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a adc configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/adc",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Channel id. If -1, use the default channel",
      "name": "channel_id",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_adc_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "float",
     "description": "ADC data read from analog signal, in milivolts(mV)",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_ADC",
   "version": "1"
  },
  {
   "aliases": [
    "light-sensor/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based light input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a light reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a light configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/light-sensor",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Channel id. If -1, use the default channel",
      "name": "channel_id",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_light_sensor_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "float",
     "description": "Light data read in lux.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_LIGHT_SENSOR",
   "version": "1"
  },
  {
   "aliases": [
    "intensity-both-sensor/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based intensity input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a light reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a light configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/intensity-both-sensor",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Channel id. If -1, use the default channel",
      "name": "channel_id",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_intensity_both_sensor_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "float",
     "description": "Unit-less light intensity.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_INTENSITY_BOTH_SENSOR",
   "version": "1"
  },
  {
   "aliases": [
    "intensity-ir-sensor/iio"
   ],
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based intensity ir input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a light reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a light configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/intensity-ir-sensor",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Channel id. If -1, use the default channel",
      "name": "channel_id",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_intensity_ir_sensor_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "float",
     "description": "Unit-less light intensity.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_INTENSITY_IR_SENSOR",
   "version": "1"
  },
  {
   "author": "Intel Corporation",
   "category": "input/hw",
   "description": "IIO based proximity input node. As any IIO device, it can use a buffer to get the readings. To use a buffer, define a size > 0 on buffer size. Readings are sent to the buffer via a trigger mechanism. Set iio_trigger_name to a valid iio trigger name in order to use buffer. If buffer is enabled but no iio_trigger_name was set, it will attempt to create a default trigger that will be activated when sending packets to TICK port.",
   "header_file": "sol-flow/iio.h",
   "in_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "any",
     "description": "Packets sent in here will trigger a proximity reading and produce packets on each of the the output ports. If buffer is enabled, current trigger must accept manual activation (default one, sysfs trigger, does).",
     "name": "TICK",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "any",
     "description": "Packets sent in here will trigger a proximity configuration attribute reading.",
     "name": "TRIGGER",
     "required": false
    }
   ],
   "license": "Apache-2.0",
   "name": "iio/proximity-sensor",
   "options": {
    "members": [
     {
      "data_type": "string",
      "description": "IIO device identifier. It's a space separated list of commands. For commands, if it's an integer value, will be interpreted as IIO device id. If it's a string starting with '/', will be interpreted as absolute path of IIO device on sysfs. If it's on the form 'i2c/X-YYYY', will evaluate to an i2c device on sysfs, where X is the bus number and YYYY is the device number, eg, 7-0069, for device 0x69 on bus 7. If it's on the form 'create,i2c,<rel_path>,<devnumber>,<devname>', where rel_path is the path of bus relative to '/sys/devices', them it will attempt to create an IIO device on that i2c bus and use it.",
      "name": "iio_device",
      "required": true
     },
     {
      "data_type": "int",
      "default": 0,
      "description": "IIO buffer size. If -1, buffering is disabled. If 0, will use default buffer size. If enabled (> 0), a trigger is necessary to perform readings. Set it using 'iio_trigger_name'; if no trigger name is set, it will attempt to create a default one, which is activated via TICK port.",
      "name": "buffer_size",
      "required": false
     },
     {
      "data_type": "string",
      "default": null,
      "description": "IIO trigger name. Name of IIO trigger that should be associated to this device for buffered readings. Set to hrtimer:<trigger name> if want to use hrtimer trigger. If not set and buffer enabled, will try to use device current trigger, if any. If none, will attempt to create a sysfs or hrtimer trigger",
      "name": "iio_trigger_name",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default scale. If false, it will attempt to use scale option.",
      "name": "use_device_default_scale",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Scale to applied to device raw readings",
      "name": "scale",
      "required": false
     },
     {
      "data_type": "boolean",
      "default": true,
      "description": "If should use device own default offset. If false, it will attempt to use offset_x, offset_y and offset_z options.",
      "name": "use_device_default_offset",
      "required": false
     },
     {
      "data_type": "float",
      "default": 0,
      "description": "Offset to be added to device raw readings",
      "name": "offset",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Sampling frequency of the sensor. If -1, use device default",
      "name": "sampling_frequency",
      "required": false
     },
     {
      "data_type": "int",
      "default": -1,
      "description": "Channel id. If -1, use the default channel",
      "name": "channel_id",
      "required": false
     },
     {
      "data_type": "drange-spec",
      "default": {
       "max": "DBL_MAX",
       "min": "-DBL_MAX",
       "step": "DBL_MIN"
      },
      "description": "Range of output packet. Usually, is the output range of used sensor. It'll be the 'min' and 'max' fields of drange packets sent on OUT port.",
      "name": "out_range",
      "required": false
     }
    ],
    "required": true,
    "version": 1
   },
   "options_symbol": "sol_flow_node_type_iio_proximity_sensor_options",
   "out_ports": [
    {
     "array_size": 0,
     "base_port_idx": 0,
     "data_type": "float",
     "description": "proximity data read.",
     "name": "OUT",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 1,
     "data_type": "float",
     "description": "Scale to applied to device raw readings",
     "name": "SCALE",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 2,
     "data_type": "float",
     "description": "Offset to be added to device raw readings",
     "name": "OFFSET",
     "required": false
    },
    {
     "array_size": 0,
     "base_port_idx": 3,
     "data_type": "int",
     "description": "Sampling frequency of the sensor. If -1, use device default",
     "name": "SAMPLING_FREQUENCY",
     "required": false
    }
   ],
   "symbol": "SOL_FLOW_NODE_TYPE_IIO_PROXIMITY_SENSOR",
   "version": "1"
  }
 ]
}
